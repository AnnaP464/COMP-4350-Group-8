# Database connection (Postgres)
DATABASE_NAME=hivehand
DATABASE_USER=hivedev
DATABASE_PASSWORD=verysafe

# JWT secrets (change these to strong random values in production)
JWT_ACCESS_SECRET=super_secret_access_key_2025
JWT_REFRESH_SECRET=super_secret_refresh_key_2025

# Application environment
NODE_ENV=development
