# .env (root)
DATABASE_NAME=hivehand
DATABASE_USER=hivedev
DATABASE_PASSWORD=verysafe
PGPORT=5432

BACKEND_PORT=4000
FRONTEND_PORT=5173
NODE_ENV=development

# Frontend will call the API via the host-mapped port
VITE_API_URL=http://localhost:${BACKEND_PORT}

# Build a Compose-time DATABASE_URL (used below)
#DATABASE_URL=postgres://${DATABASE_USER}:${DATABASE_PASSWORD}@db:${PGPORT}/${DATABASE_NAME}
